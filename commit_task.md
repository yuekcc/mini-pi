# Commit 代码

根据修改的内容提交代码，可以按需要拆分为多个 commit，不需要 push。

## 流程

1. 确认代码仓状态
2. 分析修改点（包括暂存内容和未暂存的内容）
3. 推理修改点逻辑关系
4. 起草（每个修改的）commit 消息
5. 按逻辑顺序执行 `git commit` 提交各个修改点的代码
6. 汇总报告

**NOTE**:

- 直接 commit 代码，不需要用户确认
- 优先使用简体中文起草 commit 消息
- 不需要执行 `git push`
